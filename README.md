# Rossmark Website

Static single-file redesign of rossmark.eu. Everything (fonts aside) is self-contained in `index.html` — images and photos are embedded as base64 data URIs, no build step required.

Deployed via Netlify, connected to this repo. Pushing to `main` triggers a new deploy automatically.
